import * as R from 'ramda'

import {
    FETSH_CATEGORIES_SUCCESS,
} from 'actionTypes'

const initialState = {}

export default (state = initialState, { type, payload }) => {
    switch (type) {
        case FETSH_CATEGORIES_SUCCESS:
            return R.merge(state, payload)
        default: return state
    }

}
