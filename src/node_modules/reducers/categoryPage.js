import { FETCH_CATEGORY_TYPE } from 'actionTypes'

const initialState = {
    name: 'Laptop'
}
export default (state = initialState, { type, payload }) => {    
    switch (type) {
        case FETCH_CATEGORY_TYPE:            
            return { name: payload }
        default:
            return state
    }
}